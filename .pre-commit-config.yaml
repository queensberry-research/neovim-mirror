repos:
  - repo: https://github.com/dycw/conformalize
    rev: 0.13.6
    hooks:
      - args:
          - --description=Neovim config
          - --gitignore
          - --pre-commit--prettier
          - --pre-commit--taplo
          - --readme
          - --repo-name=neovim
        id: conformalize
  - repo: https://gitea.main:3000/qrt-public/conformalize
    rev: 0.1.30
    hooks:
      - args:
          - --gitea-host=gitea.main
          - --gitea--pull-request--pre-commit
          - --gitea--pull-request--pre-commit--submodules=true
          - --gitea--push--tag
        id: conformalize
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-executables-have-shebangs
      - id: check-merge-conflict
      - id: check-symlinks
      - id: destroyed-symlinks
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args:
          - --fix=lf
      - id: no-commit-to-branch
      - id: pretty-format-json
        args:
          - --autofix
      - id: trailing-whitespace
  - repo: local
    hooks:
      - id: prettier
        name: prettier
        entry: npx prettier --write
        language: system
        types_or:
          - markdown
          - yaml
  - repo: https://github.com/compwa/taplo-pre-commit
    rev: v0.9.3
    hooks:
      - id: taplo-format
        args:
          - --option
          - indent_tables=true
          - --option
          - indent_entries=true
          - --option
          - reorder_keys=true
